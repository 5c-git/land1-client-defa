mixin layout(props)
  -
    // Подготовим строку классов
    const extraClass = props && props.class ? ' ' + props.class : ''

  section(class=`layout${extraClass}`)
    div.container
      if props.title
        div.layout__header
          h2.layout__title!=props.title
          span.layout__sub-title!=props.subtitle
      div.layout__wrapper
        block
